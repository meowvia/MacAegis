# 📋 MacAegis 产品灵感与需求备忘录 (Product Backlog & Idea Pool)

> 💡 **使用约定**：
> 1. 本文档作为日常灵感、功能探讨与需求池的集中管理清单，想到哪记到哪；
> 2. 凡是用户提示「记录」的事项，一律仅在此文档中做归档与技术评估，**绝不私自改动源码或打包发布**；
> 3. 后续开发前随时拉出清单回顾，按需挑选排期。

---

## 🚀 一、 待开发 / 下个小版本候选清单 (Next Minor Release Candidates)

### 1. 首页动态健康状态智能判定
- **需求描述**：全盘扫描后，根据系统冗余占用大小智能切换顶部健康状态与提示文案。
- **状态划分**：
  - 严重冗余（$\ge$ 5 GB）：切换为「你的 Mac 存在较多冗余占用，建议清理 ⚠️」（橙色警示）；
  - 适度冗余（500 MB $\sim$ 5 GB）：切换为「你的 Mac 发现可释放空间，建议适度优化 🧹」（蓝色图标）；
  - 极佳状态（$<$ 500 MB 或刚清理完）：保持为「你的 Mac 运行状态良好 ✨」（绿色图标）；
  - 扫描中：动态流光显示「正在深度分析系统运行环境...」。
- **当前状态**：技术方案与 UI 代码已在 `feature/ui-dynamic-health` 分支中预备就绪，等待合并发版指令。

### 2. 首页视觉层级与呼吸感排版升级
- **需求描述**：
  - 主标题字号由 24pt 增大至 28pt（大两个号），强化视觉中心；
  - 副标题文案正式规范为：「原生轻量架构 · 智能深度清理 · 隐私安全守护」；
  - 副标题与状态胶囊徽标增加独立空行留白，避免视觉拥挤。
- **当前状态**：已在 `feature/ui-dynamic-health` 备用分支中暂存。

### 3. 首页卡片网格重构与外置存储独立卡片
- **需求描述**：将外置移动硬盘（External Storage）从目前的「大文件与安装包」维度中抽离，作为独立的可视化卡片呈现，支持插拔动态感知与一键管理。
- **当前状态**：已立项，待下次首页 UI 重构时一并推进。

### 4. 「隐私隐匿」模块名称、文案与拖拽动静两态交互
- **需求描述**：
  1. 对目前顶部标签与界面的「隐私隐匿」模块名称进行审视与改名微调；
  2. **拖入区域「动静两态」交互与文案定稿**：
     - **平时（静态）**：居中展示盾牌图标与 **「拖入文件夹或文件到此处」**（*`Drop folders or files here`*），词序文件夹在前；
     - **去冗余**：彻底移除静态常驻的“松开添加”药丸按钮与下方的“将在访达中……”副标题说明；
     - **拖入悬停（动态）**：框体发光呼吸微动，文案平滑切换为定稿词汇 **「松开加入隐藏」**（*`Drop to Hide`*）；
     - **松手后**：0.1秒极速入库，底部弹出轻量 Toast 统计反馈（*“已将 N 个项目锁定并隐匿入库”*，2秒自动消失）。
- **当前状态**：已定稿并归档至下次小版本候选清单。

### 5. 「用户须知与安全免责手册」主动居中弹窗与常驻入口
- **核心定位**：防呆、免责、避坑指引（从根源上杜绝用户忘密码无法找回、迅雷下载报错甩锅等纠纷）。
- **触发与交互逻辑**：
  1. **首次使用强制感知（主动居中弹出）**：首次安装并初始化主密码进入隐私隐匿主界面时，**自动主动居中弹出**《使用须知与安全免责说明》全景毛玻璃卡片；
  2. **确认关闭**：用户点击「我已了解并妥善备份恢复码」关闭后，系统记录状态，日常不再主动打扰；
  3. **常驻优雅入口**：在界面顶部右上角（修改密码旁边）保留一个优雅的 `[ℹ️ 须知]` 或 `[❔ 免责与指南]` 按钮，点击随时重新居中唤起该手册。
- **手册三大核心内容**：
  - ① **离线加密与恢复码责任**：100% 本地硬件离线加密，无云端后门，遗忘主密码且未备份恢复码将永久无法找回；
  - ② **外部软件写入冲突避坑**：锁定目录处于系统最高物理死锁状态（000权限），严禁将迅雷/浏览器等下载路径直接设为锁定文件夹；
  - ③ **原地就地保护原理**：原地瞬时隐匿与防预览，不产生冗余拷贝，不占额外磁盘空间。
- **当前状态**：已正式立项为下次小版本高优先级必做项。

### 6. 解锁界面 Touch ID 图标替换为 Apple 官方经典红粉色质感
- **需求描述**：
  - 目前解锁旁边的 Touch ID 指纹图标使用的是绿色，视觉不够高级；
  - 调整为 1:1 复刻 Apple 官方标准 Touch ID 视觉（经典的苹果红粉色渐变/洋红珊瑚色 `Color(hex: "FF3B30")` / `Color(hex: "FA2D48")` 与标准 SF Symbol `touchid` 矢量符号）。
- **当前状态**：已记录归档至下次小版本候选清单。

---

## 🧠 二、 深度思考与方案待定池 (Inspiration & Discussion Pool)

### 4. 隐私隐匿离开后的防遗忘与安全上锁策略
- **背景痛点**：
  - 用户解锁后若切标签就立即强行锁死，会打断正在进行的文件操作（引发第三方软件报错或恐慌）；
  - 若永远不锁，用户容易遗忘导致私密文件长期暴露。
- **备选方案池**：
  - **方案 A（系统级兜底 ⭐）**：Mac 盖盖 / 锁屏 / 系统休眠 / 退出 App（Cmd+Q）时，自动触发 `lockAll()` 安全隐匿；
  - **方案 B（视觉醒目提示）**：再次进入隐私隐匿页面时，若存在已解锁文件，顶部常驻暖黄色警示条并提供 `[一键全部隐匿 🔒]` 快捷按钮；
  - **方案 C（灵活配置）**：在偏好设置中提供「离开自动锁定时间」开关（从不 / 5分钟 / 15分钟 / 锁屏休眠时）。
- **当前状态**：技术方案已归档，等待后续决策。

### 5. 第三方下载工具（如迅雷）与锁定文件夹的协同问题
- **背景现状**：外部下载工具向处于 `0o000 + uchg` 锁定的目录写入数据时，会被 macOS 内核物理拦截并报错。
- **探讨方案**：通过普通中转收件箱监听 + 毫秒级后台原子事务搬运入库。
- **当前决策**：为防止复杂外部并发写入导致出错，**克制功能膨胀，暂时不碰，保持当前纯手动显式解锁/锁定的确定性逻辑**。

---

## 💎 三、 商业化与 Pro 版权益预留 (Future Monetization Reserves)

- **免费版核心定位**：全功能垃圾清理、大文件扫描、应用卸载残留分析、基础隐私隐匿额度（例如 3~5 个重要文件夹）、菜单栏全景监控。
- **未来 Pro / 买断版潜在权益储备**：
  - 无限制文件/文件夹隐匿容量；
  - 外置移动硬盘一键整盘隐匿；
  - 自动锁屏与紧急隐匿快捷键；
  - 双重诱骗金库（假密码登录展示无关文件夹）；
  - 高级硬件传感器历史温控曲线。

---

## 📌 协作规范备忘
- **口令触发**：凡是用户提到「记录 / 记录一下 / 记在备忘录」，一律写入本文档归档，**严禁私自修改主分支源码或构建安装包**；
- **执行触发**：只有用户明确下达「动手 / 开干 / 开始做」时，方可进入代码实现流程。
