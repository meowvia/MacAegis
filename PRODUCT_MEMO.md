# 📋 MacAegis 产品灵感与需求备忘录 (Product Backlog & Idea Pool)

> 💡 **协作执行铁律**：
> 1. 本文档为 MacAegis 全局**唯一需求与灵感归档池**；
> 2. 凡用户提及「记录 / 记录一下」，一律仅在此文档中做归档与方案推敲，**严禁私自修改主分支源码或构建发布包**；
> 3. 后续开发前随时说一声「拉出清单」，逐条核对并下达「开干 / 动手」指令后再正式执行。

> 📌 **版本演进与发版纪律 (Version Roadmap)**：
> - **当前在线版本**：`v0.2.0`；
> - **下一个版本定为 `v0.2.1`（稳健修复、深度清理与应用卸载加固版）**：
>   - 包含：6 大工业级安全底线修复、顶栏与交互 Bug 修复、文案去您改你、网络代理矩阵；
>   - 包含：**【第六节】系统清理进阶引擎**（APFS 快照平滑释放、FCP 渲染缓存、Steam 碎片深度规则）；
>   - 包含：**【第七节】应用彻底卸载 v2.1**（Entitlements 签名解析 App Group、7 天卷感知隔离区、1-Click 还原体检报告）；
> - **`v0.3.0` 立项时机**：未来启动 Apple 官方 Liquid Glass 全局流体玻璃升级或更大改版时再立项；
> - **商业化定调**：**目前完全没有商业化考虑**，保持纯粹自主研发与极致体验。


---

## 🚀 一、 未来版本候选清单 (Future Release Candidates - 储备池)


### 1. 首页四大核心维度彻底重构（废弃大文件，融入全谱系高占用工程废料）
- **战略定位**：
  - **彻底废弃「大文件」无脑扫描**：电影、视频、私人相册是用户的个人数字资产而非垃圾，无脑按体积扫描既侵犯隐私（杜绝扫描出个人电影引发的社死痛点），又无实际清理价值；
  - **外置存储无需独立成卡**：存储只是介质，废料本质相通。外置盘中的剪辑渲染缓存、Steam 中断碎片、隐藏废纸篓，按业务属性平滑融入现有的 4 张经典卡片中（插盘自动纳入统计并在明细中标注 `[外置盘]`，拔盘自动回缩仅统计主盘），保持界面架构极致纯净、对称与自洽；
- **当前状态**：已确立为最终核心架构，随下次首页 UI 重构一并推进。


### 2. 全局 UI 图标与视觉成熟度大排查
- **需求描述**：全面排查 App 各个模块中偏轻佻、幼稚的图标和装饰性元素，逐个清除并重构为 Apple 原生严肃、沉稳、克制的 SF Symbols 体系。
- **当前状态**：已记录，等待后续逐项过筛清理。

---

## 🧠 二、 深度思考与方案待定池 (Inspiration & Discussion Pool)

### 1. 隐私隐匿离开后的防遗忘与安全上锁策略
- **背景痛点**：
  - 用户解锁后若切标签就立即强行锁死，会打断正在进行的文件操作（引发第三方软件报错或恐慌）；
  - 若永远不锁，用户容易遗忘导致私密文件长期暴露。
- **备选方案池**：
  - **方案 A（系统级兜底 ⭐ 推荐）**：Mac 盖盖 / 锁屏 / 系统休眠 / 退出 App（Cmd+Q）时，自动触发 `lockAll()` 安全隐匿；
  - **方案 B（视觉醒目提示）**：再次进入隐私隐匿页面时，若存在已解锁文件，顶部常驻暖黄色警示条并提供 `[一键全部隐匿 🔒]` 快捷按钮；
  - **方案 C（灵活配置）**：在偏好设置中提供「离开自动锁定时间」开关（从不 / 5分钟 / 15分钟 / 锁屏休眠时）。
- **当前状态**：技术方案已归档，等待后续决策。

### 2. 第三方下载工具（如迅雷）与锁定文件夹的协同问题
- **背景现状**：外部下载工具向处于 `0o000 + uchg` 锁定的目录写入数据时，会被 macOS 内核物理拦截并报错。
- **探讨方案**：通过普通中转收件箱监听 + 毫秒级后台原子事务搬运入库。
- **当前决策**：为防止复杂外部并发写入导致出错，**克制功能膨胀，保持当前纯手动显式解锁/锁定的确定性逻辑**。

### 3. 功能模块命名统一重构（“隐私隐匿” vs “保险箱” 术语割裂与新名待定）
- **背景现状与痛点**：
  - 目前该模块在导航栏中被称为「隐私隐匿」（Privacy Conceal），但在恢复密钥提示中又出现了「这是你**保险箱**的专属恢复密钥...」，存在术语割裂；
  - 之前探讨过“隐藏隐匿”或“隐私隐匿”四字略显生硬晦涩，而“保险箱”又容易让用户误以为是传统高发热重度加密（与我们“零读写原生轻量系统罩”定位产生偏差）；
- **当前状态**：**技术与文案已先期归档挂起，等待用户敲定最终统一命名**；
- **后续执行约定**：待用户正式确定最满意的新名称后，全工程将侧边栏、标题、按钮、注释、说明弹窗及恢复密钥文案等所有关联处，**一次性全链路批量统一替换为新名称**，彻底根除称谓割裂。


---

## 🧪 三、 远期大版本与深度技术储备 (Future Reserves - v0.3.0+)

> 💡 **核心定位**：**目前完全没有商业化考虑**。本项目专注打造属于 Mac 用户自己的纯粹原生、极致轻量、真正安全的系统工具。本章节所有项目仅作为未来 `v0.3.0+` 大版本时的功能与视觉技术储备，当前 `v0.2.1` 绝不提前引入。

### 1. 远期高级功能候选池 (v0.3.0+)
- 外置移动硬盘一键整盘隐匿；
- 自动锁屏与紧急安全隐匿快捷键；
- 双重诱骗金库（假密码登录展示无关安全文件夹）；
- 高级硬件传感器历史温控曲线。

### 2. 【重大改版视觉储备 · 留待 v0.3.0+】全面适配 Apple Liquid Glass 官方流体玻璃设计语言
- **技术可行性评估结论（Gemini 3.1 Pro 审查确认）**：
  1. **总体可行性极高 (95%+)**：得益于 MacAegis 从第一天起坚持 100% 纯原生 SwiftUI 架构（零 Electron / Flutter 杂质），标准组件编译原生继承系统特性，自定义组件仅需使用官方 `GlassEffectContainer` 聚合容器包裹即可，迁移成本极低；
  2. **性能损耗架构扛得住**：光影折射、动态模糊与流体融合（Morphing）对 GPU 瞬时计算略增，但依靠官方指定的 `GlassEffectContainer` 合并单通道渲染，加之 `MacAegisCore` 核心业务与 UI 彻底解耦，界面的 GPU 渲染绝不阻塞文件锁定与底层清理；老旧系统具备原生降级保护；
  3. **App 包体积 0 增长**：纯系统级 SDK 驱动，零外部框架（如 Lottie）或大贴图依赖，所有的液态玻璃材质与折射依靠几行纯 Swift 代码修饰符实现，安装包稳守极致轻量的 1.5MB~ 级别（Release DMG < 2.0MB）；
- **发版规划与决策**：
  - **当前 v0.2.1 坚决不碰**：下一个版本 `v0.2.1` 坚决做减法，只修复 Bug 与加固安全红线，保持现有成熟 UI 不变；
  - **未来 v0.3.0+ 重大改版时启动**：待后续确定启动大改版或引入重大新功能时，再统一将全 App 视觉跃迁至 Liquid Glass。
- **当前状态**：**已作为远期设计资产归档，现阶段完全搁置。**



---

## 📦 四、 极致轻量打包规范 (Strict Lightweight Packaging Rules)

1. **打包前物理级清空**：每次打包前必须完整重建 `.app` 目录，严禁任何历史重命名残留（杜绝多份二进制进包）；
2. **强制执行标准 Strip**：所有 Release 二进制在签名打包前，必须执行 `strip -u -r` 剔除无用调试符号；
3. **严守体积红线**：Release DMG 严格控制在 **2.0 MB 以内**，ZIP 严格控制在 **1.5 MB 以内**，绝不塞入任何多余冗余，贯彻「原生轻量架构 · 零负担」的产品灵魂。

---

## ✍️ 五、 文案、排版与视觉成熟度规范 (Design & Tone Principles)

1. **讲目的，不讲过程**：直说达成了什么效果，不要过度解释底层如何实现，更不要渲染夸大目的；
2. **彻底摒弃夸张与绝对化表达**：
   - 坚决弃用“0 常驻负担”、“绝对安全”、“全网最强/最深”、“史诗级”等虚浮词汇；
   - 替换为中立、严谨的事实陈述（如 *“低资源占用”、“退出即释放”、“按需唤起”*）；
3. **保留客观可测的事实表达**：
   - *“秒级响应”、“毫秒级隐匿”* 等具备真实系统调用基准测试支撑的客观性能描述可以保留；
4. **淡化固定体积宣传**：
   - 不再将具体的安装包体积数字作为重点宣传口号，随着底层隔离区（Quarantine）、Entitlements 签名解析等能力的演进，体积会有自然的动态变化；重点强调 *“纯原生 Swift 开发、无多余第三方依赖”* 即可；
5. **全面去 Emoji 与花哨符号**：彻底停用杂乱的 Emoji 与装饰性图标，杜绝二次元或轻浮感，保持专业工具的严肃成熟；
6. **图标去幼稚化**：界面内部图标严格对标 macOS 系统原生应用标准，成熟、极简、克制；
7. **全链路统一执行**：无论是代码注释、UI 界面文本、README 文档还是对外宣传，全链路一律遵守此严谨务实的表达准则；
8. **人称代词中性自然（全面去“您”改“你”）**：
   - 坚决摒弃带有客服感或冗余尊称的“您”字，全面对标 macOS 系统原生中性、沉稳、平等的对话风格，文案统一使用“你”；
   - **现存待替换点位归档（下次统一处理）**：
     1. [`LargeFileRules.swift:58`](file:///Volumes/2T/MacAegis/Sources/MacAegisCore/Rules/LargeFileRules.swift#L58)：*“建议您确认...”* $\rightarrow$ *“建议你确认...”*；
     2. [`SettingsView.swift:276`](file:///Volumes/2T/MacAegis/Sources/MacAegisApp/Views/SettingsView.swift#L276)：*“均在您的 Mac 本地执行...”* $\rightarrow$ *“均在你的 Mac 本地执行...”*；
     3. [`PrivacyVaultView.swift:496`](file:///Volumes/2T/MacAegis/Sources/MacAegisApp/Views/PrivacyVaultView.swift#L496)：*“这是您保险箱的专属恢复密钥...”* $\rightarrow$ 待功能最终统一新命名确定后全链路对齐（见第二节第 3 条）；

     4. [`PrivacyVaultView.swift:592`](file:///Volumes/2T/MacAegis/Sources/MacAegisApp/Views/PrivacyVaultView.swift#L592)：*“为保障您的数据安全...”* $\rightarrow$ *“为保障你的数据安全...”*；
     5. [`PrivacyVaultView.swift:602`](file:///Volumes/2T/MacAegis/Sources/MacAegisApp/Views/PrivacyVaultView.swift#L602)：*“保护您的私密数据...为您生成...协助您安全找回”* $\rightarrow$ 全部统改统一为“你”。



---

## 🧹 六、 系统清理引擎进阶升级蓝图 (Deep Cleaner Architecture Blueprint)

> 💡 **核心目标**：在保持“零常驻负担、纯原生轻量、绝不误杀”的前提下，将可释放空间深度与真实性提升至行业第一梯队（对标 CleanMyMac 与腾讯柠檬）。

### 1. 【P0 级 · 严谨度修复】权限前置探测（消除“扫出却清不掉”的信任崩塌）
- **核心痛点**：`/private/var/log` 等 root 目录或 Xcode 模拟器受限路径若被标为直接可清理，普通用户权限清理会静默失败，极伤用户信任（“扫得到清不掉”）。
- **落地方案**：
  - 扫描时增加 `FileManager.default.isWritableFile(atPath:)` 快速校验，无直接写权限项不混入常规一键清理；
  - Xcode 模拟器残留引导执行 `xcrun simctl runtime delete` 进行系统级注销，避免产生孤儿 plist。

### 2. 【P1 级 · 释放量破局】补齐全谱系高占用工程与系统废料（突破数百 GB 空间瓶颈）
- **① APFS 本地快照（10~80GB ⭐ 性价比单项冠军）**：
  - macOS “系统可清除空间”的主要来源；
  - 采用系统标准调用 `tmutil thinlocalsnapshots / 10000000000 4` 触发系统级平滑释放，**0 数据风险，瞬间释放巨量空间**；
- **② 影视创作与多媒体工程巨兽（20~200GB+ ⭐ 生产力场景最大吞噬者）**：
  - **Final Cut Pro**：扫描 `.fcpbundle` 内的历史 `Render Files/High Quality Media`（高画质 ProRes 渲染中间件）与光流分析文件，片子剪完后可安全释放巨量空间；
  - **剪映专业版 (CapCut) / 达芬奇 (DaVinci Resolve)**：草稿预览切片、`CacheClip` 优化媒体中间件；
  - **Adobe Premiere / After Effects**：`Media Cache Files`、`Video Previews` 预览切片；
  - **Logic Pro / 音频宿主**：冻结音轨渲染生成的巨型临时 WAV/AIFF 文件；
- **③ 游戏平台与虚拟环境暗黑残留（10~80GB+）**：
  - **Steam 库（内外置全盘）**：`steamapps/downloading/`（异常中断或拔盘遗留的巨型孤儿分块碎片）、`temp/`、`shadercache/` 着色器残留；
  - **Epic Games / 战网**：未完成更新的预分配孤儿安装包；
  - **主机模拟器 (Ryujinx/RPCS3 等)**：预编译显卡着色器与固件解包临时包；
  - **Docker / 虚拟机**：`Docker.raw` 稀疏镜像感知与安全 shrink 引导，VMware/Parallels 挂起镜像；
- **④ 外置硬盘专属幽灵废纸篓（10~100GB+ ⭐ 移动盘假满元凶）**：
  - 外置移动硬盘根目录下隐藏的 `/.Trashes`（用户随手删除但未清空的幽灵文件，锁在外置盘占用巨量空间）；
- **⑤ iOS 本地设备备份（10~50GB）**：
  - 深度扫描 `~/Library/Application Support/MobileSync/Backup`，精准识别老旧 iPhone/iPad 备份，展示明细供用户显式确认；
- **⑥ Apple Mail 邮件附件缓存（5~20GB）**：
  - 覆盖 `~/Library/Containers/com.apple.mail/Data/Library/Mail Downloads`，清理重度邮件用户遗留的巨量附件临时副本；
- **⑦ 现代开发构建与包管理器（5~30GB）**：
  - Xcode `DerivedData` 编译衍生数据、旧版真机调试符号（`iOS DeviceSupport`）；
  - Homebrew 旧版本 formulae 堆积（支持安全调用 `brew cleanup --prune=all`）。


### 3. 【P2 级 · 性能与体验优化】
- **状态栏轮询降频**：主窗口隐藏/后台时，将硬件与磁盘 IO 轮询频率自适应降为 4~5 秒，真正贯彻“零常驻开销”。
- **文件体积计算性能提升**：大目录遍历改用 `URLResourceKey.totalFileAllocatedSizeKey` 批量批次获取，使几万级小文件目录的扫描速度提升 3~5 倍。

### 4. 【P3 级 · 次级安全增强】
- **日志保留期策略**：`~/Library/Logs` 默认只清理 30 天以上的陈旧日志，保留近期崩溃日志供排查；
- **缓存安全时间窗**：对非确定性缓存目录叠加“修改时间 > 7 天”再放行。

---

## 🗑️ 七、 应用彻底卸载与隔离引擎终极架构蓝图 (Deep Uninstaller Architecture Blueprint v2.1)

> 💡 **核心认知与纪律**：
> 1. 用户要的不是冷冰冰的路径，而是**“确定感”**（删了会怎样）。
> 2. **恪守措辞纪律**：严禁使用“100%、绝对彻底、全网最深”等虚夸词汇，坚持**“承诺什么，就必须能提供对应的证据链验证”**。

### 1. 【L0 权限层 · 权限归因修正与分流】
- **双权限状态感知**：明确区分 **完全磁盘访问权限 (FDA)** 与 **App Management (应用管理权限，macOS 13+ 独立 TCC 类别)**；
- **失败归因精准分流**：
  - 若错误属于 TCC / AppManagement 拦截 $\rightarrow$ 引导跳转至系统设置授权页面（`x-apple.systempreferences:com.apple.preference.security?Privacy_AppManagement`）；
  - 若错误属于 `/Library` 下真正的 `root:wheel` 属主文件 $\rightarrow$ 触发单次管理员提权执行（提权仅严格限定在 root 属主范围，绝不滥用为常规删除兜底）；
- **设置页状态呈现**：在偏好设置中提供 FDA 与 App Management 的实时状态指示行。

### 2. 【L1 情报层 · 三信号正交检测与 Entitlements 解析】
- **信号 ① Entitlements 签名解析（核心突破）**：
  - 读取主二进制代码签名的 entitlements，提取所有 `com.apple.security.application-groups` 与 `keychain-access-groups`，精准直接命中 `~/Library/Group Containers/group.*` 共享目录；
- **信号 ② TeamID 签名关联**：
  - 通过 `SecCodeCopySigningInformation` 提取 Apple 开发者 TeamID，对散落未声明 Entitlements 的同团队附属文件进行全域关联；
- **信号 ③ BundleID 按段正交匹配**：
  - 支持 `bId == sub`、前缀/后缀级联匹配；
- **18 大全域深度候选目录**：
  - 覆盖 `~/Library/Group Containers`、`~/Library/Application Scripts`、`~/Library/HTTPStorages`、`~/Library/Cookies` 以及 `/private/var/folders/`（动态读取 `DARWIN_USER_CACHE_DIR` 提取以 BundleID 命名的系统级临时与缓存）；
- **体积真相引擎**：
  - 对 Universal 双架构与内嵌 Frameworks 嵌套 Bundle 进行深度递归展开遍历；
  - 统计权限跳过项并在 UI 呈现：*“X MB（另有 Y MB 因权限未计入 ⓘ）”*，确保账目 100% 可验算；
- **白名单拦截显式化**：
  - 被安全白名单保护的项目显示为 *“N 项被安全白名单拦截（可查看）”*，绝不静默消失。

### 3. 【L2 终止层 · 全谱系进程与系统扩展处理】
- **系统扩展 Pre-flight 检查（实测核心踩坑点）**：
  - 检测 `Contents/Library/SystemExtensions` 与 `PlugIns`；
  - 针对含网络扩展/代理类应用（结合 `utun` 活跃作为旁证），在卸载前弹出专属引导：*“请先在应用内断开网络连接并退出”*；
- **LaunchDaemons / LaunchAgents**：
  - 使用 `launchctl bootout` 进行系统级优雅卸载；
- **SMAppService + BTM 登录项**：
  - 针对 macOS 13+ 新机制，注销 Background Task Management 登录项幽灵开关；
- **cfprefsd 偏好设置精准清理（严禁 killall cfprefsd）**：
  - 执行 `defaults delete <bundleId>`，通过 XPC 协议通知系统偏好守护进程优雅释放指定域缓存，**严禁执行 `killall cfprefsd`**（杜绝误伤其他正在保存配置的后台无辜进程导致数据丢失）；
- **LaunchServices 注销**：
  - 执行 `lsregister -u /Applications/Foo.app` 立即消除访达“打开方式”中的幽灵图标。

### 4. 【L3 执行层 · 独立隔离区式安全卸载 (超越废纸篓)】
- **执行顺序**：严格遵循 **“先卫星数据，后主程序包”** 的原子顺序；
- **隔离式卸载（挂载卷感知 Volume-Aware 同源技术）**：
  - **主盘系统卷**：被删除项移入 `~/Library/Application Support/MacAegis/Quarantine`；
  - **外置硬盘卷**：就地在该外置盘根目录下创建隐藏的 `.MacAegisQuarantine` 文件夹进行隔离，**确保隔离操作始终是零拷贝、同卷微秒级的原子 Rename 瞬时完成**，杜绝跨盘大文件拷贝延迟与中途拔盘产生坏死碎片；
  - 通过 xattr 记录原始绝对路径与元数据；
  - **7 天安全保留期**：保留 7 天，到期自动物理清除；
  - **1-Click 完整还原**：支持一键完整无损还原（包含主程序与深层附属文件），彻底消除废纸篓易被用户随手清空或受 TCC 限制的缺陷。

### 5. 【L4 验证层 · 卸载体检报告 (证据链闭环)】
- 卸载完成后即时呈现 **《卸载体检报告》** 证据链清单：
  - 进程终止 `[✓]` / 扩展注销 `[✓]` / 自启动服务 bootout `[✓]` / cfprefsd 缓存注销 `[✓]`；
  - 关联 N 项 M MB 已全部移除 `[✓]`；
  - K 项被白名单安全拦截（支持点击查看明细）；
  - 实际释放 X MB vs 预估 Y MB（附带差异归因说明）。

---

## 🛡️ 十一、 工业级安全基线与架构修正清单 (Gemini 3.1 Pro 对抗审查与安全首要原则落地)

> 💡 **核心共识**：经 Gemini 3.1 Pro 对全套源码与架构规划的对抗审查，一致确立以下 **6 大安全首要原则**，作为 MacAegis 后续开发与优化的最高安全红线：

### 1. 【隐匿安全】彻底删除 XOR 混淆，回归真正“0 字节修改”
- **现状危害**：`PrivacyVaultManager` 中的 `transformHeader` 读写文件前 64KB 进行异或，存在断电、崩溃或外置盘拔出导致的不可逆物理损坏风险，违背“不读写、不破坏、仅加系统防护罩”的初衷；
- **修正方案**：彻底剔除 `transformHeader` 及所有异或混淆代码。隐匿与锁定 100% 依赖系统内核原生 `lchflags(..., UF_HIDDEN | UF_IMMUTABLE)` + `0o000` 权限 + Xattr 元数据标记。实现真正的 0 字节文件改动，耗时由几十毫秒降至微秒级，断电 0 风险。

### 2. 【系统安全】全面阻断软链接（Symlink）穿透锁定
- **现状危害**：`chflags()` 和 `chmod()` 在 Darwin 内核中默认穿透软链接。若用户将指向 `/System`、`/Library` 或 `~/Documents` 的快捷方式拖入隐匿，会穿透锁死系统核心目录或整个文稿库；
- **修正方案**：使用 `lchflags()` 替代 `chflags()`，配合 `lstat()` 前置探测软链接，遇到软链接直接阻断越权或仅锁定软链接本身。

### 3. 【文件系统】卸载隔离区实行“挂载卷感知”（Volume-Aware）
- **现状危害**：外置盘应用若移动至主盘 `~/Library/Application Support/MacAegis/Quarantine`，跨磁盘卷移动无法使用原子 `rename()`，会触发极慢的物理复制与删除，中途拔盘会导致海量碎片与数据死锁；
- **修正方案**：探测文件所属挂载点（Mount Point）。外置盘文件就地在所属外置盘根目录下创建隐藏的 `.MacAegisQuarantine/` 隔离，确保全生命周期均为同卷零拷贝原子瞬时操作。

### 4. 【系统稳定性】严禁使用 killall cfprefsd
- **现状危害**：`cfprefsd` 是全局偏好服务，暴力杀死会导致同一瞬间其他无辜后台应用（如 Pages、Xcode、系统设置）正在提交的配置写入丢失；
- **修正方案**：严禁 `killall cfprefsd`。仅调用原生 `defaults delete <BundleID>`，由系统通过 XPC 优雅安全地通知释放目标缓存。

### 5. 【硬件寿命】机械硬盘高频轮询彻底剥离，改用事件驱动
- **现状危害**：`DashboardViewModel` 每 1.5 秒定时调用 `fetchMountedDrives()` 遍历磁盘，会导致休眠中的机械移动硬盘（HDD）磁头频繁被唤醒，加剧硬件磨损并引发系统周期性“彩虹圈”卡顿；
- **修正方案**：将磁盘 I/O 探针从 1.5 秒 Timer 中彻底剥离。改用 `NSWorkspace.didMountNotification` 与 `didUnmountNotification` 监听物理插拔事件，平时 0 轮询 0 I/O 开销，按需响应。

### 6. 【资产保护】卸载应用废弃盲目 SIGKILL，保护未保存劳动成果
- **现状危害**：应用未退时 1 秒后直接调用 `app.forceTerminate()`（SIGKILL），会导致用户未保存的文档瞬间蒸发；
- **修正方案**：卸载前调用温和的 `.terminate()`（SIGTERM），若 2 秒后仍在运行，坚决不滥杀，立即停止卸载流程并弹窗提示：“应用正在运行且可能有未保存的工作，请手动保存退出后再继续”，保障用户资产绝对安全。




---

## 🎨 八、 首页状态语义与交互体验进阶蓝图 (Dashboard State Semantics & UX)

> 💡 **核心定位**：首页视觉骨架（中央球体焦点 + 4 卫星卡 + 单一 Primary CTA）已达到行业成熟水准无需推倒；进阶重点在 **“状态机三态化”** 与 **“证据驱动的健康判定”**，解决“未扫描前宣判健康”和“4 个 0 KB 像数据坏了”的信任细节。

### 1. 【P0 级 · 状态机三态化与 0 KB 信任修补】
- **核心痛点**：
  - 用户刚启动尚未扫描时，首页直接宣判“运行状态良好 ✨”且 4 张卡全显示“0 KB”，容易造成两种误解：
    1. 真实 Mac 绝不可能 0 缓存，未扫就说良好属于“无证据宣判”；
    2. 全显示 0 KB 视觉上像数据渲染失败或网络/系统卡死。
- **三态交互策略**：
  - **① 未扫描态 (Pending)**：
    - 主标题：*“点击一键扫描，深度分析可释放空间”*；
    - 中央球体：*“开始分析”* 或待命光晕；
    - 4 卫星卡：数值处显示 *“待扫描”*（而非生硬的 0 KB）。
  - **② 扫描中态 (Scanning)**：
    - 卫星卡动态流转显示 *“分析中...”*，球体环呈现脉冲进度。
  - **③ 已完成态 (Completed)**：
    - 才展示具体统计数值（如 2.4 GB），并根据真实 `safeReclaimable` 给出对应的健康评估（良好 / 建议优化 / 严重冗余）。

### 2. 【P0 级 · FDA 权限门控与防误报】
- **权限与 0 值的关联**：
  - 当扫描结果接近 0 KB 时，前置探测系统 **完全磁盘访问权限 (FDA)**；
  - 若用户未授权 FDA（导致 `~/Library` 很多受限目录扫不到），**绝不宣判“很干净”**，而是明确提示：*“授予完全磁盘访问权限以获得精准扫描结果 ℹ️”*。

### 3. 【P1 级 · 宏观磁盘水位上下文】
- 在副标题或球体下方提供一行极简的磁盘宏观背景（如 *“Macintosh HD 剩余 72.6 GB / 245.1 GB”*），让用户清楚知道自己电脑的真实剩余水位。

### 4. 【P2 级 · 细节打磨与商业感闭环】
- **Result Card 结果闭环**：清理完成后在顶部优雅展示本次实测释放量（如 *“已成功释放 12.4 GB 🚀”*）并提供废纸篓直达；
- **菜单栏快捷动作**：在状态栏卡片中预留 `[⚡ 一键清理]` 与 `[🔒 一键隐匿]` 快捷入口；
- **主操作按钮去箭头**：移除「一键极速清理」按钮后方的 `>` 符号（因为它是直接就地执行清理动作，不是进入下一级的页面导航，去掉箭头更符合 macOS 原生按钮规范）。


### 5. 【P1 级 · 扫描结果时效性与智能过期 (TTL & State Invalidation)】
- **真实运行规律**：系统与浏览器每分每秒都在产生新缓存（刷网页、开 App 会立刻生成临时文件），即使刚清理完为 0 KB，切走几分钟后再回来也绝不可能永远是 0 KB；
- **行业标杆解法（结果时效性机制）**：
  - 记录 `lastCleanedAt: Date` 并在结果上带时间戳（如 *“已于 10:30 完成清理 · 状态极佳”*）；
  - **TTL 过期衰减（15~30 分钟）**：当切标签或最小化一段时间再切回时，若已过有效期，界面自动优雅流转为 *“上次清理于 20 分钟前 · 点击重新扫描”*，绝不长久死挂 0 KB 假象，保持专业工具的数据新鲜感与真实性。

### 6. 【Bug 归档 · 现存交互缺陷】清理完成浮动 Toast 遮挡首页 Mac 状态提示
- **现象描述**：点击「一键极速清理」完成后，弹出的 Toast 浮层（`actionToastMessage`）位于页面正顶部中央，正好物理重叠遮挡了首页主标题与系统状态评估（`health.title`，如“你的 Mac 运行状态良好”），造成约 2.5 秒的文本压制与闪烁干扰；
- **根因分析**：[`DashboardView.swift`](file:///Volumes/2T/MacAegis/Sources/MacAegisApp/Views/DashboardView.swift#L40-L57) 根 `ZStack` 中的 `Floating Toast Notification` 与下方 `masterReferenceCockpitView` 的顶栏标题均设定为 `.padding(.top, 16)` 居中布局，在垂直和水平坐标系上完全重叠，且 Toast 带有实体卡片背景与阴影，直接覆盖在 28pt 状态大标题正上方；
- **最终确定决策**：**采纳方案 B（融入 Result Card 闭环，不额外占用呈现）**
  - **实现逻辑**：
    1. 清理执行完成时，彻底废弃原有的正上方浮动 Toast 弹窗（`actionToastMessage`）；
    2. 顶栏状态区就地平滑流转为 **Result Card 结果态**，直接在原有标题区域展示本次释放成果（如 *“已成功清理释放 12.4 GB 空间”*，副标题带 *“已移至系统废纸篓”*）；
    3. 状态行右侧或下方提供极简原生的 `[打开废纸篓 ↗]` 快捷动作按钮；
    4. 既达成了清晰的结果反馈与废纸篓闭环，又彻底消除了悬浮层的物理遮挡，全界面视觉纯净克制。

### 7. 【终极确立】四大维度定义重构、外置存储平滑整合、尺寸黄金放大与气泡动作一元化
- **1. 首页 4 张卡片终极业务定义（内外置全盘通收，彻底消灭大文件与社死痛点）**：
  - **左上 ①：系统维护与底层快照**：APFS 本地快照平滑释放、系统运行日志与诊断报告、Apple Mail 附件临时副本、系统组件运行缓存；
  - **左下 ②：工程创作与开发构建（彻底取代原大文件）**：
    - 针对 Final Cut Pro ProRes 渲染缓存（`.fcpbundle/Render Files`）、剪映草稿预览切片、PR/达芬奇 `Media Cache Files`、Xcode `DerivedData`、构建中间件；
    - **100% 聚焦于高占用工程废料，彻底剔除个人视频与电影扫描，从根源上杜绝任何隐私泄露与社死隐患**；
  - **右上 ③：安装包与游戏下载碎片**：DMG/PKG 安装镜像、Steam 异常中断孤儿分块（`steamapps/downloading`）、临时解压包、浏览器与网络流静态缓存；
  - **右下 ④：已卸载残留与全盘废纸篓**：应用卸载残留沙盒目录、**外置移动硬盘隐藏 `.Trashes`**、本机废纸篓、失效开机启动项；
- **2. 外置存储平滑整合（无额外卡片，零冗余）**：
  - 存储介质天然归一：外置盘插入时，其高占用工程缓存、Steam 碎片、外置 `.Trashes` 自动汇入上述对应 4 张卡片统一呈现，明细中标注 `[外置盘]`；拔出外置盘自动平滑回缩为仅统计主盘；
  - 界面永远维持经典的左右 2+2 对称构图，零冗余卡片，零突兀感；
- **3. 【本版搁置 · 留待 v0.3.0+】尺寸等比黄金放大**：
  - **实测与决策**：昨晚效果图实测表明，仅需在背景中适当点缀极其轻量的星尘微光，旷野感就已经自然消除，无需大动干戈去改动核心控件的几何尺寸；
  - **发版纪律**：`v0.2.1` 作为纯稳健修复版，**坚决保持现有的球体（230pt）与卡片（210pt）成熟尺寸不变**，杜绝大改 UI 尺寸带来的布局抖动与未知风险；尺寸放大方案参数完整归档，留待未来 `v0.3.0+` 大改版时再统一推敲；
- **4. 清理动作一元化（融入中央球体 · 留待 v0.3.0+ 评估）**：
  - 彻底移除球体正下方外挂的胶囊按钮，中央大球集「点击开始分析」与「点击立即清理」于一身（三态自洽流转），作为未来界面演进储备；


### 8. 【Bug / 原生体验修复 · P1 级】顶栏边框栏（Titlebar）缺乏 macOS 原生双击缩放/最小化交互
- **现状与缺陷证实**：
  - 在 [`MacAegisApp.swift`](file:///Volumes/2T/MacAegis/Sources/MacAegisApp/MacAegisApp.swift#L73) 中窗口声明了 `.windowStyle(.hiddenTitleBar)`；
  - 顶部边框栏 [`MainView.swift`](file:///Volumes/2T/MacAegis/Sources/MacAegisApp/Views/MainView.swift#L44-L50) 中的 `headerBar` 纯由自定义 SwiftUI 视图构建，只响应了 Logo 点击、Tab 切换和设置齿轮等子组件的单次点击；
  - **在全工程中检索，`performZoom` 出现次数为 0，`headerBar` 空白与背景区域未监听任何双击手势**；
  - **违背 macOS 原生直觉**：在所有标准 Mac 软件（Safari、Finder、Xcode、系统设置）中，用户双击顶栏空白区域时，系统会自动执行「缩放/铺满 (Zoom/Fill)」或「最小化到程序坞 (Minimize)」；但在 MacAegis 中双击边框栏完全毫无反应，产生明显的失灵与非原生顿挫感；
- **落地修复方案**：
  - 在 `headerBar` 背景与空白区域绑定双击手势：
    ```swift
    .onTapGesture(count: 2) {
        guard let window = NSApp.keyWindow else { return }
        let action = UserDefaults.standard.string(forKey: "AppleActionOnDoubleClick") ?? "Maximize"
        switch action {
        case "Minimize":
            window.miniaturize(nil)
        case "None":
            break
        default:
            window.performZoom(nil)
        }
    }
    ```
  - 自动遵循系统设置（桌面与程序坞 -> “连按窗口标题栏以...”）的真实偏好，让顶栏交互瞬间对齐 Apple 原生顶级手感。

### 9. 【视觉与顶栏精细打磨 · v0.2.1】顶栏一体化渐变融合、标签强化、标题下沉舒展与前台微呼吸星尘
- **1. 导航标签适度放大强化**：
  - 将「智能清理 / 应用卸载 / 隐私隐匿」的图标由 11pt 提升至 **13pt**，字号由 12pt 提升至 **13pt 粗体**，胶囊内边距适当舒展，彻底消除小气感，让核心导航定位清晰立住；
- **2. 彻底抹杀硬黑分割线，顶栏背景流体渐变交融**：
  - 彻底删除 `MainView.swift` 中的 `Divider().opacity(0.2)` 硬分割线；
  - 顶栏底色采用高透材质，与下方深邃画布通过**垂直微羽化渐变（LinearGradient Fade）自然融为一体**，消除横向截断感；
- **3. 状态大标题纵向下移舒展（获得充裕呼吸留白）**：
  - 标题“你的 Mac 运行状态良好”整体纵向下移 **20 ~ 26pt（相当于两到三行行距）**，彻底告别紧顶导航条的拥挤感，形成从容优雅的 Apple 顶级空灵层次；
- **4. 前台专属“微呼吸星尘背景”（让软件真正活着，0 负担）**：
  - 在暗色宇宙画布中点缀细腻的随机星芒微粒，叠加 **4 秒超慢周期的纯 GPU Alpha 微呼吸动效（Opacity 0.35 ~ 0.65 柔和起伏）**；
  - **生命力与零负担兼备**：前台操作时呼吸脉动，App 切后台或窗口失焦瞬间毫秒级自动冻结，CPU 0.0% / GPU 0.0%，既灵动又克制。








---

## 🌐 九、 流量与网络代理模式探测引擎重构蓝图 (Network & Proxy Monitor Blueprint)

> 💡 **核心定位**：彻底解决 v2rayN / Clash / Surge 切换分流与 TUN 模式时“识别不准、甚至短路误报直连”的历史顽疾，改用**正交信号聚合 + 运行时真相源**架构。

### 1. 【核心根因归因 · 为什么之前 v2rayN 怎么测都不准】
- **① 目标错位**：代码读取的是 `guiNConfig.json` 中的 `SysProxyType`。这个字段**只代表客户端是否接管系统代理开关**，当用户在 v2rayN 内部切换“绕过大陆”与“全局”路由规则时，`SysProxyType` 根本不会改变，导致指示器永远无法识别出全局代理；
- **② 短路链漏洞（TUN 盲区）**：当开启 TUN 透明代理时，v2rayN 通常会将系统代理清零（`SysProxyType = 0`），之前的逻辑直接返回 `.direct`，**从而直接短路截断了后面的 `utun` 虚拟网卡检测**，导致全流量都在走隧道，界面却瞎报“普通直连”。

### 2. 【P0 级 · 正交信号聚合决策架构】
收集 3 路独立物理信号，通过确定性矩阵进行聚合决策，坚决废弃层叠 short-circuit 链：
- **信号 A**：系统网络代理开关（`SCDynamicStore` / HTTP / SOCKS 127.0.0.1）；
- **信号 B**：虚拟链路接口状态（`utun` / `ppp` / `ipsec` 处于 UP + RUNNING 状态）；
- **信号 C**：已知代理核心进程存活（`xray` / `v2ray` / `mihomo` / `clash` / `Surge`）。

| 信号 A (系统代理) | 信号 B (TUN 隧道) | 信号 C (核心进程) | **最终确定模式** | 视觉表现 |
| :---: | :---: | :---: | :--- | :--- |
| Any | **True (开启)** | Any | **`.tunnel` (TUN 隧道加速)** | 🔵 蓝青色指示器 |
| **True (开启)** | False | Any | 探测路由段：<br>• 有直连/分流规则 $\rightarrow$ **`.rule` (规则分流)**<br>• 无直连规则 $\rightarrow$ **`.global` (全局代理)** | 🟢 翠绿色<br>🔴 鲜红色 |
| False | False | **True (在运行)** | **`.clientIdle` (客户端就绪/直连)** | ⚪ 灰色/浅蓝 |
| False | False | False | **`.direct` (普通直连)** | 🔵 天蓝色 |

### 3. 【P1 级 · 性能与事件驱动改造】
- **事件驱动取代无脑轮询**：接入 `SCDynamicStore` 系统网络配置变更回调通知，实现系统代理切换时**毫秒级即时响应**；
- **进程扫描缓存**：对 `sysctl` 内核进程表检查施加 3~5 秒短期缓存，彻底消除 1.0s 频繁扫描进程表的 CPU 开销，严守零常驻负担。

---

## 🔍 十、 严谨中立的自我代码评审机制与反自满工程纪律 (Self-Review & Anti-Complacency)

> 💡 **核心警醒**：严禁养成“言之凿凿、浮夸口嗨、盲目自满”的不良习惯。单靠单元测试通过绝不等于可以交货，必须以极其严谨、客观、行业标准的态度主动审查，绝不浪费用户的宝贵时间。

### 1. 【彻底杜绝“盲目自满与交货错觉”】
- **测试通过 $\neq$ 达到工业标准**：单元测试仅验证了 Mock 理想数据下的基础逻辑，绝不代表能抵抗 macOS 真实复杂的真实环境（如 `cfprefsd` 内存复活、`Entitlements` App Group 漏扫、`0o000` 在 exFAT 上的失效、TUN 虚拟网卡短路等）；
- **严禁虚假承诺**：禁止使用“100%、绝对完美、全网无敌”等浮夸营销词汇，恪守工程师实事求是的本色。

### 2. 【建立主动的前置「对抗性代码自审」流程】
- **前置自我把关**：在完成功能或交付前，必须**主动以极其苛刻的挑剔视角自审一轮**（涵盖权限边界、真实文件系统兼容性、系统扩展阻断、并发竞态、行业标杆竞品对比）；
- **主动暴露短板**：不等用户去外部寻求评审，在开发阶段就主动查摆隐藏断点，把风险与解法前置讲清，最大程度节约用户的沟通与决策成本。

### 3. 【证据驱动与真实对齐】
- 凡涉及体积、路径、权限等能力，必须用真实系统命令（`du`、`stat`、`codesign`、`launchctl`）进行真实验证与客观数据支撑；
- 明确讲清“当前已覆盖了什么”、“还有哪些未覆盖与边界限制”，不夸大、不掩盖。











